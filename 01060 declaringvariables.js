'use strict';

// Declare the variable message and assign the value 'Hello' to it
let message = 'Hello';

console.log(message);

// Assigning new value to already existing variable. It does not need to be "let" or declared again
message = 'How are you?';

console.log(message);
