"use strict";

let days = ['Sunday','Monday','Tuesday'];

// indexing of an array starts at 0
console.log(days[0]);

let day = days[1];

console.log(day);

let heights = [2.3, 3.2, 1.8, 2.7];

console.log(heights[2]);

for(let height = 0; height <heights.length; height++ ) {
    console.log(heights[height]);
}